import React from "react";
import ScheduleCard from "../cards/schedulecard";

const Schedule = () => {
  return (
    <div>
      <ScheduleCard />
      <ScheduleCard />
    </div>
  );
};

export default Schedule;
